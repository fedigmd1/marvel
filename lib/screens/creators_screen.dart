import 'package:flutter/material.dart';

class CreatorsScreen extends StatelessWidget {
  static const routeName = '/creators';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
